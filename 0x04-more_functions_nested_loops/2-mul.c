#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers.
 * @a: first integer
 * @b: second integer
 * Return: Return the result of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
